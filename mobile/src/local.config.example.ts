/**
 * Local development network settings.
 * Copy from local.config.example.ts if this file is missing.
 */
export const USE_PHYSICAL_DEVICE = true;

/** Your Mac's LAN IP — run: ipconfig getifaddr en0 */
export const LAN_BACKEND_HOST = '192.168.0.105';
